export async function emptyActivity(): Promise<void> {
  // Empty function to ensure the file is not empty
  // This app does not use Activities, so this function is not used
  // Keeping it here to show a typical Worker setup with Activities
  return;
}
